/**
 * JavaScript entry point that imports from the built distribution
 * This is a workaround for environments that can't handle TypeScript directly
 */
export { default } from './dist/index.js';
export * from './dist/index.js';
